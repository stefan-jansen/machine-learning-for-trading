## Quantopian Alpna Factor Library

The notebook [factor_library](factor_library.ipynb) contains a large mumber of alpha factor candidates that we can use as features in ML models on the Quantopian platform.

