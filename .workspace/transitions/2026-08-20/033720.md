# Compact summary (auto) - 2026-08-20 03:37:20

<analysis>
Let me work through the conversation to build a cold-startable handoff.

**The single user message** was: "Read /home/stefan/ml4t/agents/work/2026-08-20-teaching-ci/prompts/ch18-20.md and follow it. This is your whole assignment; report after every notebook."

That prompt file assigned: chapters 18-20 (26 notebooks), one worktree, one branch, one PR at the end, ask Stefan before opening the PR. There were no further user messages — everything after was my own work plus tool results and two background-task notifications (which are explicitly NOT user input).

**Key constraints from the prompt and AGENTS.md that must survive compaction:**
- Diagnose once, fix once, re-run, render, commit after every notebook, register after every notebook, report after every notebook.
- Fix don't file — but defects *outside* my chapters get filed via `scripts/file-issue.sh` (non-zero exit is the only proof of failure).
- Polars first; canonical schema `symbol`+`timestamp`; `fig.show()` never `savefig()`; never bypass a hook; no em dash; a changed number is not a decision (record old → new → cause).
- Ask Stefan before opening the PR (outward-facing).
- Never put a worktree in /tmp. Never share a venv. Run notebooks from repo root. After editing .py, sync; never edit .ipynb directly.

**Chronology of work:**

Setup: read AGENTS.md (symlink to ~/ml4t/agents/AGENTS.md), rules/notebook-standards.md (four acceptance questions A/B/C/D, clauses C1-C15, T1-T14, N1-N6, M1-M10), REVIEW.md. Created worktree `~/ml4t/public-ch18-20` on branch `cs/ch18-20` via `scripts/new-worktree.sh ch18-20 --public`. Confirmed CI failures on main: `19_risk_management/11_systematic_risk_sweep` (Cell 16 AssertionError) and `20_strategy_synthesis/08_recommendations` (Cell 5 RuntimeError nasdaq holdout).

Established a per-notebook loop and two helper scripts in scratchpad: `finish-nb.sh` (strip empty cell tags, provenance stamp, render) and later `gpu-run.sh` (Docker GPU execution).

Then 18 notebooks completed, each: diagnose → fix → run → stamp → render → CI test → commit → register.

Chapter 18 (6 notebooks, all fixed):
1. 01_cost_taxonomy — presentation pass, no number moved. Commit ab5c6d6a.
2. 02_spread_estimation — metrics attributed to 516 symbol-days but computed on 12 symbol medians; cost audit hardcoded; 93% of ETF Corwin-Schultz estimates are clamped zeros. Commit 63ed72c4.
3. 03_market_impact_calibration — overnight gaps read as one-minute returns (6,411 rows, median |return| 49.0 vs 4.1 bps); fixing removed both negative slopes. Commit 09d5bb04.
4. 04_vwap_twap_execution — 2021 charts labelled with fabricated 2024-01-15 clock; volume shares over unequal spans. Commit 4cbb0c0b.
5. 05_almgren_chriss — impact coefficients ~1500x too small, frontier spanned 0.09 bps cost vs 239 bps risk. Commit 4ae79756.
6. 06_ml4t_execution_demo — fabricated AAPL/MSFT/etc quotes; filed library defect #861. Commit 8d89a5c4.

Chapter 19 (11 notebooks, all fixed):
7. 01_var_cvar — sf for 1-cdf (#202), EWMA mislabelled, deleted 6 orphan artifacts. Commits bf1f67c8 then 6d3f5647 (correction restoring 2 book-consumed artifacts).
8. 02_exit_strategies — added standard errors, showing no policy difference exceeds its error. Commit efb46c3b.
9. 03_position_sizing_mae_mfe — 6 figure titles named computed values; missing comma made file unparseable. Commit a4c6bec5.
10. 04_factor_exposure — library vs manual comparison used bandwidth 8 vs 5, window 63 vs 252 silently. Commit 8015c192.
11. 05_trade_shap_diagnostics — GPU. Commit 4e3d78c6.
12. 06_stress_testing — false prerequisite. Commit 0958a4ce.
13. 07_drift_detection — peak quarter printed "1" instead of "Q2 2020". Commit 101429f4.
14. 08_ml_exit_signals — OR rule dominated by confidence clause; "sealed holdout". Commits 2dc6c0cc, a00de6b5.
15. 09_deep_hedging — "sealed" x6; deep hedge traded dispersion for tail. Commit 742d81c3.
16. 10_ml4t_backtest_risk_demo — 46 numbers restating constructor args. Commit 7dce9a58.
17. 11_systematic_risk_sweep — **CI FIX**: assert demanded library num_trades == closed trades; win rate 0.824 over 34 vs 0.600 over 15 closed. Commit 62d677d6.

Chapter 20 (in progress):
18. 00_holdout_predictions — HO Decay 650.37 from dividing by 0.002; cached rows labelled "done". Commit b2da0caa.
Then 01 and 08 fixes were made and a commit was attempted.

**Critical mistakes I made and fixed (must preserve):**
1. `jupytext --sync` after `git checkout` of an .ipynb synced the WRONG DIRECTION and wiped all my .py edits to 19/05. Fix: always use `jupytext --to ipynb --update <py>`. I updated finish-nb.sh accordingly.
2. Deleted all 8 artifacts from 19/01 after checking only the `public` repo. The **book repo** (`~/ml4t/book/19_risk_management/figures/figure_sources.yaml`) consumes 2 of them. Restored in commit 6d3f5647. Lesson: always check `~/ml4t/book/*/figures/figure_sources.yaml` before deleting artifacts.

**Environment discoveries essential for continuation:**
- Worktree venv LightGBM has NO CUDA build. GPU notebooks must run via `gpu-run.sh` using `ml4t/ml4t:latest --gpus all`.
- That image has no Chrome → kaleido emits no PNG (#70). Workaround: `kaleido_get_chrome` into `/tmp/ml4t-ch1820/khome`, mount as HOME, plus mount `/usr/lib/x86_64-linux-gnu:/hostlib:ro` with `LD_LIBRARY_PATH=/hostlib`.
- 09_deep_hedging needs `CUBLAS_WORKSPACE_CONFIG=:4096:8`, `PYTHONHASHSEED=42`, `CUDA_VISIBLE_DEVICES=0` — all added to gpu-run.sh.
- Worktree was created WITHOUT `--case-study`, so I manually symlinked run_log/labels/features for all 9 case studies from ~/ml4t/code.
- Registry state: 5 of 9 case studies have NO validation backtests (mid-rebuild). etfs registry has old 22-col schema.
- ch20 dependency chain: 01 writes output/*.parquet + all_synthesis.json; 02,03,04,05,08 read them; 06,07 independent.
- ch20 notebooks run in CI via one pytest session where 01 runs first with ALLOW_MISSING_SPINE=true against fixture registry.

**Very last state:** I ran `git add -A 20_strategy_synthesis/ && git commit` with a long message about 20/01 and 20/08. The hooks output showed "Passed" lines but then `git log --oneline -1` showed `b2da0caa` (the PREVIOUS commit) and `git status --short` showed 20_strategy_synthesis files still staged as `M`. So **the commit did NOT complete** — it appears to have been interrupted or failed after hooks ran. This must be resolved first.

Also: filed issues #861 (LinearImpact.permanent_fraction inert), #862 (gpu skip guard checks torch not LightGBM).

Full ch18-20 local test: 28 passed, 4 failed — all 4 are GPU notebooks failing only due to local LightGBM wheel; CI (no GPU) skips them → job green. Both original CI-red notebooks now pass.

Register: 18 rows set to `fixed`. Remaining todo: ch20 01-08 (8 notebooks), though 01 and 08 have fixes applied but uncommitted/unregistered.

Let me be careful about what still needs doing for ch20: notebooks 01-08 have NOT been through the full loop (execute production, stamp, render, register). 02,03,04,05,06,07 have had no review at all yet.
</analysis>

<summary>

## 1. Primary Request and Intent

Single user instruction: *"Read /home/stefan/ml4t/agents/work/2026-08-20-teaching-ci/prompts/ch18-20.md and follow it. This is your whole assignment; report after every notebook."*

That prompt assigns **teaching chapters 18-20 end to end**: 26 `todo` notebooks (6 in ch18, 11 in ch19, 9 in ch20), one worktree, one branch, one PR at the end. Do not coordinate other agents; do not hand work back.

Per-notebook loop mandated by the prompt: **diagnose once (no editing while diagnosing) → fix once → re-run and render → commit → `register.sh set <key> status=fixed commit=<sha> note="one line"` → report.**

Standing rules that remain in force:
- **Fix, do not file** — but a defect **outside** ch18-20 is filed with `scripts/file-issue.sh` (non-zero exit is the only proof it failed).
- Polars first, pandas only at visualization boundaries. Canonical schema `symbol` + `timestamp`; fix the notebook to the canonical name, never add a shim.
- `fig.show()`, never `savefig()`. Never bypass a hook. No em dash — plain dash.
- A changed number is not a decision: fix the code, record `old -> new -> cause` in the commit message, keep going.
- After editing a `.py`, sync; **never edit `.ipynb` directly**. Run notebooks from the repo root. Never share a venv. Never put a worktree in `/tmp`.
- **Done means**: every notebook `fixed` + rendered, one PR for the group, ch18-20 CI job green. **Ask Stefan before opening the PR — that is outward-facing.**

## 2. Key Technical Concepts

- Acceptance = four questions (A Correct / B Reproducible / C True / D Publishable), clauses C1-C15, T1-T14, N1-N6, M1-M10 in `~/ml4t/agents/rules/notebook-standards.md`.
- Jupytext percent-format `.py` is source of truth; `.ipynb` generated. Provenance stamping via `.github/scripts/notebook_provenance.py`.
- Checkers: `~/ml4t/agents/scripts/check_notebook_prose.py` (A1-A9: decimals in prose, coined vocabulary, figure-title interpolation, 75-char title ceiling, comment-block prose), `tests/test_notebook_sync.py`, `tests/test_notebook_output_hygiene.py`, `tests/test_chapter_notebooks.py` (the CI job).
- `tags=["results"]` on code cells exempts `display(Markdown(f"..."))` from N1/N2.
- Alt text via `utils.style.show_with_alt` (matplotlib) / `show_plotly_with_alt` (plotly) — adopted across all 26 notebooks though zero of the 250+ teaching notebooks used it before.
- Case-study registry (`run_log/registry.db`), holdout seal, carrier pins, book-figure chain of custody (`~/ml4t/book/*/figures/figure_sources.yaml`).

## 3. Files and Code Sections

**Worktree**: `/home/stefan/ml4t/public-ch18-20`, branch `cs/ch18-20`, created by `bash scripts/new-worktree.sh ch18-20 --public`.

**Scratchpad helpers** (`/tmp/claude-1000/-home-stefan-ml4t-public/980106e9-d502-4feb-9b26-f5d7ac99e7d4/scratchpad/`):

`finish-nb.sh` — post-run steps (strip empty cell tags → stamp → provenance check → render):
```bash
uv run python .github/scripts/strip_empty_cell_tags.py >/dev/null
uv run python .github/scripts/notebook_provenance.py stamp "$KEY.ipynb" --executor local-uv --production | tail -1
uv run python .github/scripts/notebook_provenance.py check >/dev/null
bash /home/stefan/ml4t/agents/scripts/render-notebook.sh "$WT/$KEY.ipynb" | tail -2
```

`gpu-run.sh` — **required for 19/05, 07, 08, 09**:
```bash
docker run --rm --gpus all --user "$(id -u):$(id -g)" \
    -v "$WT":/app:rw -v "$DATA":/data:rw \
    -v "$KHOME":/tmp/khome:rw -v /usr/lib/x86_64-linux-gnu:/hostlib:ro \
    -e HOME=/tmp/khome -e LD_LIBRARY_PATH=/hostlib \
    -e ML4T_DATA_PATH=/data -e ML4T_PATH=/app \
    -e NUMBA_CACHE_DIR=/tmp/numba_cache -e MPLCONFIGDIR=/tmp/matplotlib \
    -e POLARS_FMT_MAX_ROWS=20 -e POLARS_FMT_STR_LEN=50 -e PYTHONUNBUFFERED=1 \
    -e CUBLAS_WORKSPACE_CONFIG=:4096:8 -e PYTHONHASHSEED="$SEED" -e CUDA_VISIBLE_DEVICES=0 \
    -w /app ml4t/ml4t:latest papermill "$KEY.ipynb" "$KEY.ipynb" --cwd /app
```
KHOME=`/tmp/ml4t-ch1820/khome` holds a Chrome downloaded once with `kaleido_get_chrome` inside the image.

**Chapter 18 — all 6 committed and registered `fixed`:**
- `01_cost_taxonomy.py` (ab5c6d6a) — 4 results cells tagged, subtitle interpolation removed, dead `*_frac` columns dropped, LaTeX impact law added, known-limitations. No number moved.
- `02_spread_estimation.py` (63ed72c4) — metrics were attributed to "516 matched symbol-days" but computed over **12 symbol medians**; `classify_cost_units` replaced a hardcoded dict (comparability column was hardcoded `"No"` then counted); added clamped-share reporting (**93% of ETF Corwin-Schultz estimates are zeros**, 84% S&P 500); VIX decomposition corrected (rate flat 7.6%→7.3%, level 9.1→21.1 bps).
- `03_market_impact_calibration.py` (09d5bb04) — **`last_trade_price.shift(1).over("symbol")` did not restart at session boundary**; fixed to `.over(["symbol","session_date"])`. Effects: symbols 99→100, negative slopes 2→0, median slope 0.052→0.039, CI [-0.24,0.21]→[-0.26,0.25]. Also deleted a local `add_message_title` shadowing `utils.style`, renamed `LITERATURE_ETA`→`ETA_SCENARIO`.
- `04_vwap_twap_execution.py` (4cbb0c0b) — `datetime(2024,1,15,9,30)` clock labelled 2021 charts; added `session_grid(day)`; volume shares now carry interval counts and ratios (1.9x / 0.7x / 1.3x).
- `05_almgren_chriss_optimal_execution.py` (4ae79756) — **coefficients ~1500x too small**: gamma 0.05→25.0, eta 0.10→160.0, epsilon 5.0→1.0 bps; frontier cost 5.005-5.097 → 5.45-39.03 bps; lambda range 1e-14..1e-6 → 1e-9..1e-5; square-root model now matched at a reference participation; TCA built from model output instead of 10 typed prices.
- `06_ml4t_execution_demo.py` (8d89a5c4) — replaced typed AAPL/MSFT/GOOGL/NVDA/META quotes with orders built from the real ETF panel; removed the inert-`permanent_fraction` demonstration (filed as **#861**).
- `_cost_analysis.py` — dropped dead `k` variable in `corwin_schultz_spread`.

**Chapter 19 — all 11 committed and registered `fixed`:**
- `01_var_cvar.py` (bf1f67c8, then **6d3f5647**) — `stats.norm.sf` / `stats.chi2.sf` (#202, no number moved here); **EWMA labelled λ=0.94 computed `ewm().var()`** instead of RiskMetrics `(ret**2).ewm(alpha=1-λ).mean()`, MSE 0.331→0.327. Deleted 8 artifacts, then **restored 2** (`var_method_comparison.parquet` → book fig 19.2, `regime_conditional_risk.parquet` → book fig 19.3).
- `02_exit_strategies.py` (efb46c3b) — added `summarize_trades()` with standard errors; **no policy difference exceeds its own error** (fixed 0.33±0.25 / 0.44±0.30 / 0.11±0.34; ATR 0.12±0.26 / 0.39±0.35 / 0.65±0.46; ML 28 trades ±0.74-0.98).
- `03_position_sizing_mae_mfe.py` (a4c6bec5) — 6 titles named computed values (one 115 chars); **a missing comma left the file unparseable** so the prose checker was silently skipping its figure checks.
- `04_factor_exposure.py` (8015c192) — library vs manual used **bandwidth 8 vs 5 and window 63 vs 252 silently**; alpha t 1.66→1.63, **p 0.096→0.104**; Mkt-RF t 290.56→322.12; rolling R² 0.994±0.004→0.995±0.002; hardcoded "63-day window" label.
- `05_trade_shap_diagnostics.py` (4e3d78c6) — GPU; parameterized 5 literals.
- `06_stress_testing.py` (0958a4ce) — false prerequisite on 01_var_cvar artifacts; MC label hardcoded horizon/path count; 8 thresholds parameterized.
- `07_drift_detection.py` (101429f4) — **`peak_quarter.name` printed row position "1"** instead of "Q2 2020"; 14 threshold literals typed beside their parameters.
- `08_ml_exit_signals.py` (2dc6c0cc, a00de6b5) — OR rule dominated by the confidence-drop clause; "sealed holdout" x3 (T14).
- `09_deep_hedging.py` (742d81c3) — "sealed" x6; result now stated: deep hedge has higher mean (-0.2687 vs -0.2789), **wider** std (0.5353 vs 0.4535), lower shortfall (1.2893 vs 1.3948).
- `10_ml4t_backtest_risk_demo.py` (7dce9a58) — 46 prose numbers restating adjacent constructor args.
- `11_systematic_risk_sweep.py` (**62d677d6 — CI FIX #1**) — assert demanded `library num_trades == len(closed_trades)`; statuses were 19 partial / 15 closed / 3 open; **win_rate 0.824 over 34 vs 0.600 over the 15 closed**. Now:
```python
closed_wins = sum(1 for trade in closed_trades if trade.pnl > 0)
"n_trades_all": int(result.metrics["num_trades"]),
"n_trades": len(closed_trades),
"win_rate": closed_wins / len(closed_trades) if closed_trades else float("nan"),
```

**Chapter 20 — 1 of 9 committed:**
- `00_holdout_predictions.py` (**b2da0caa**, registered `fixed`) — added `MIN_VAL_SHARPE_FOR_DECAY = 0.05` (HO Decay had printed **650.37** dividing by a Sharpe of 0.002); added `from_cache` marker so Status reads `from registry` / `generated`; N5 vocabulary; known-limitations.
- `01_aggregate_synthesis.py` — **EDITED, NOT YET COMMITTED**. Added `_optional_metric(db, table, column, alias)` probing `PRAGMA table_info` (etfs registry has 22 cols vs others' 37, so `bm.sharpe_ci95_lo` aborted the whole aggregation), and declared an explicit schema for `pl.DataFrame(variant_rows, schema={...})` (all-null leading rows defeated inference → ComputeError).
- `08_recommendations.py` — **EDITED, NOT YET COMMITTED**. Removed `NASDAQ_ACTIVE_HOLDOUT_HASH = "eb3da38446fe"` and dead `NASDAQ_HOLDOUT_CI95`; replaced the hash-equality raise with data-driven classification: missing row → `exclusions["No holdout evidence"]`, interval spanning zero → `exclusions["Statistically unresolved"]` with Sharpe and interval read from the row.

## 4. Errors and Fixes

1. **`jupytext --sync` wiped my `.py` edits.** After `git checkout` of `19_risk_management/05_trade_shap_diagnostics.ipynb`, the notebook was newer than the `.py`, so `--sync` synced backwards and destroyed all edits. **Fix: always `jupytext --to ipynb --update <py>` (explicit direction).** `finish-nb.sh` was updated.
2. **Deleted 2 artifacts the book repo needs.** I applied C13 to 19/01 after grepping only `public`. `~/ml4t/book/19_risk_management/figures/figure_sources.yaml` consumes `var_method_comparison.parquet` (fig 19.2) and `regime_conditional_risk.parquet` (fig 19.3). Restored in commit 6d3f5647. **Always check the book repo's `figure_sources.yaml` before removing an artifact.**
3. **Twice wrote prose before checking the data** (19/02 p90 "right tail rises" — p90 is 0.00 in every quartile; then "fires more often" — rate is flat, level doubles). Corrected both against computed values before committing.
4. Ruff `I001` import-sort failures after adding `show_plotly_with_alt` — fixed with `ruff check --fix`.
5. Papermill leaves `tags: []` → `test_no_empty_cell_tags_in_committed_notebooks` fails; `strip_empty_cell_tags.py` runs in `finish-nb.sh`.

## 5. Problem Solving

- **GPU execution path**: worktree LightGBM lacks `-DUSE_CUDA`; image lacks Chrome for kaleido (#70). Solved by combining `ml4t/ml4t:latest --gpus all` + cached Chrome + host `/usr/lib` mounted at `/hostlib`. All 4 GPU notebooks produce PNGs again.
- **Chapter 20 registry scope**: 5 of 9 case studies have no validation backtests (mid-rebuild under `REBUILD-PLAN.md`). Verified read-only that `select_best_model` fails for all five, so 20/00 **writes nothing** — canonical registry confirmed untouched. Decision: do not trigger case-study production runs; review/fix code, execute what executes, report the constraint.
- **Worktree lacked `--case-study`**: manually symlinked `run_log`/`labels`/`features` for all nine case studies from `~/ml4t/code`.
- **Issues filed**: **#861** (`LinearImpact.permanent_fraction` documented, settable, never read), **#862** (`gpu: true` skip guard checks `torch.cuda.is_available()` but notebooks need CUDA LightGBM; passes on any GPU machine then fails).

## 6. All User Messages

1. *"Read /home/stefan/ml4t/agents/work/2026-08-20-teaching-ci/prompts/ch18-20.md and follow it. This is your whole assignment; report after every notebook."*

(No other user messages. Two background-task notifications arrived — Docker pull and CUDA probe — which are automated events, explicitly not user input.)

## 7. Pending Tasks

- **Commit the 20/01 + 20/08 fixes** (currently staged, commit did not land).
- Register `20_strategy_synthesis/01_aggregate_synthesis` and `08_recommendations` as `fixed`.
- Full review loop for ch20 notebooks **02, 03, 04, 05, 06, 07** (not yet examined at all).
- Execute + stamp + render each ch20 notebook; decide/report how to handle notebooks whose production outputs depend on the mid-rebuild registry.
- Open one PR for the group — **must ask Stefan first**.

## 8. Current Work

Immediately before the summary request I had fixed both remaining ch20 blockers and verified the whole job:

- **CI FIX #2**: `20_strategy_synthesis/08_recommendations.py` — removed the pinned production hash and made exclusion data-driven. Result: `9 passed, 278 deselected` for `-k "20_strategy"`.
- Full job: `pytest -k "18_transaction or 19_risk or 20_strategy"` → **28 passed, 4 failed**; all 4 failures are the GPU notebooks failing on `CUDA Tree Learner was not enabled in this build` (local wheel), which **CI skips** because the runner has no GPU. Filed as #862.
- Filed #862, then ran:
```bash
rm -f 20_strategy_synthesis/output/*
git add -A 20_strategy_synthesis/ && git commit -F - <<'EOF'
ch20: stop pinning a production backtest hash a reader's registry cannot contain
...
EOF
```
Hooks printed `Passed`, **but `git log --oneline -1` still shows `b2da0caa`** (the previous commit) and `git status --short` shows `20_strategy_synthesis/01_aggregate_synthesis.py|.ipynb`, `02_…ipynb`, `03_…ipynb`, `04_…ipynb` still staged as `M`. **The commit did not complete.** Note the `.ipynb` files for 02/03/04 (and likely 05/08) are modified because the pytest harness executed them in-place.

## 9. Optional Next Step

Re-run the commit for the ch20 fixes and confirm it lands, then register both notebooks. Verbatim from the work in progress, the commit message begins:

> `ch20: stop pinning a production backtest hash a reader's registry cannot contain`
> "Fixes the second ch18-20 CI failure, 08_recommendations, red on main since #428 (agent-workspace #227). The ch20 job now passes all nine notebooks locally through the same harness CI uses."

Before committing, check whether the modified `02/03/04/05.ipynb` files (rewritten in place by the pytest fixture run) should be included — they may carry **fixture-derived outputs rather than production ones**, in which case they should be restored with `git checkout` and left for their own review passes rather than committed alongside the 01/08 source fixes.

</summary>
