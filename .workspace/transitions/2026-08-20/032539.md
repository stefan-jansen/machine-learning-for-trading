# Compact summary (auto) - 2026-08-20 03:25:39

<analysis>
Let me work through this conversation to build a cold-startable handoff.

**The single user message** (there is only one real user turn, plus one system-injected instruction to respond with text only): "Read /home/stefan/ml4t/agents/work/2026-08-20-teaching-ci/prompts/ch16-17.md and follow it. This is your whole assignment; report after every notebook."

That prompt file assigned: chapters 16-17 of the ML4T public repo, 24 todo notebooks (14 in ch16, 10 in ch17), one worktree, one branch, one PR at the end, diagnose the red `ch16-17` CI job first, report after every notebook.

**Setup phase**: I read AGENTS.md (identical in both `~/ml4t/public` and `~/ml4t/agents`), `rules/notebook-standards.md`, `REVIEW.md`, the `notebook-figures` skill. Created worktree `~/ml4t/public-ch16-17` on branch `cs/ch16-17` off `origin/main` (9c2a7159) using `--public` (the prompt omitted it, which would have branched the archived `~/ml4t/code` repo). Later added case-study symlinks (etfs, crypto_perps_funding, cme_futures) for run_log/labels/features because I hadn't used `--case-study`.

**CI diagnosis**: two failures, both in ch16 — `08_signal_method_comparison` (FileNotFoundError on crypto registry.db) and `17_backtrader_zipline_engine_parity` (ValueError: artifact scenario label is not canonical). Both fixed; both now pass locally; full ch16 suite is 18/18 green.

**Per-notebook loop established**: diagnose → fix .py → `jupytext --sync` → `jupyter nbconvert --execute --inplace` → `notebook_provenance.py stamp --production` → `render-notebook.sh` → commit → `register.sh set ... status=fixed`.

**Chapter 16 complete (18/18 fixed)**. Chapter 17: 01 blocked, 02/03/04 fixed, 05 in progress, 06/08/09 pending, 11/12/13 unprobed (GPU/tier-skipped).

Key recurring defect families found: display(Markdown(f"...")) in untagged code cells (N2), figure titles interpolating computed values (N4), Title Case headings, missing known-limitations lists (T7), dead `YIELD_CURVE_SLOPE`/`dgs10-dgs2` fallbacks, hardcoded seeds alongside SEED, home-directory/Dropbox path fallbacks (M3), silent live-rerun fallbacks, single-bar/zero-length-bar charts (T5), and pinned hashes against files not in the repo.

Substantive correctness findings worth preserving: ch16/06 cost mismatch (5bp vs 5+5bp); ch16/07 fill ordering inert in both share regimes (false claim); ch16/09 benchmark trading through the strategy's 14-bar warmup (22.6% BTC head start); ch16/10 crisis CVaR shallower not fatter; ch16/12 variance_trials=0.5 vs measured ~1.0; ch16/14 `_etf_baseline.py` not actually NB01's simulator (four differences, now ported, parity test added); ch16/17 scenario-label conflation + provenance block that never existed; ch17/02 max-Sharpe holds 2 of 30; ch17/03 min-variance/min-CDaR concentrate harder than max-Sharpe (contradicted my own first prose); ch17/04 min_wealth_multiple 0.10 at 46.5x leverage under a "survive" title.

Also: ml4t-diagnostic 0.1.1 changed Sortino (downside deviation over all obs vs negative subset) — every Sortino in both chapters moves on re-run. I initially wrote "no computed value moved" for ch16/01, then caught and corrected it in a follow-up commit.

Two filed/registered items: agent-workspace#860 (seed_results fixture inconsistency), and ch17/01 registered `blocked`.

Outstanding decision I flagged to the user and have not acted on: teaching-chapter alt text (`show_plotly_with_alt`) — corpus-wide gap across 26 chapters, deliberately out of scope, and I said I'd file an infra issue for it. **I have not actually filed that issue yet.**

The immediate work at cutoff: ch17/05, mid-way through replacing `display(Markdown(...))` blocks. Five remain (lines ~433, ~788, ~812, ~972, ~1030), ruff is currently failing with F821 on `display`/`Markdown` because I removed the import before converting all call sites. The notebook has NOT been re-executed or committed.

Let me be careful to note the exact state: `05_factor_allocation_evidence.py` is currently in a **non-compiling state** (F821 x10). That's the most important thing for the next agent.

Also uncommitted: `08_library_comparison` and `09_allocator_comparison` .py+.ipynb have the Timestamp fix applied and synced but are not committed (git status showed them modified).
</analysis>

<summary>

## 1. Primary Request and Intent

One user message, verbatim:

> Read /home/stefan/ml4t/agents/work/2026-08-20-teaching-ci/prompts/ch16-17.md and follow it. This is your whole assignment; report after every notebook.

That prompt assigns **chapters 16-17 of the ML4T public repo, end to end**: one worktree, one branch, one PR at the end, no coordinating other agents, no handing work back. 24 notebooks are `todo` (14 in ch16, 10 in ch17). It directs: diagnose the red `ch16-17` CI job first; per notebook run diagnose-once → fix-once → re-run → render → commit → register with a one-line note; report after every notebook; fix rather than file inside the chapters; a changed number is not a decision (record old → new → cause); never bypass a hook; Polars first; canonical `symbol`+`timestamp`; `fig.show()` never `savefig()`; no em dash.

**Done means**: every notebook in the group `fixed` in the register and rendered, one PR opened, and the `ch16-17` CI job green. **Ask Stefan before opening the PR; that is outward-facing.**

## 2. Key Technical Concepts

- **Governing docs (only three)**: `~/ml4t/agents/rules/notebook-standards.md` (four acceptance questions A/B/C/D, clauses C1-C15, T1-T14, N1-N6, M1-M10), `~/ml4t/agents/REVIEW.md` (the procedure), `~/ml4t/agents/REGISTER.tsv` (one row per notebook).
- **Register**: `bash scripts/register.sh set <key> status=fixed commit=<sha> note="..."` run from `~/ml4t/agents`.
- **Render**: `bash scripts/render-notebook.sh <abs path>.ipynb` → `~/ml4t/review/<dir>_<stem>.html` + `.blob` sidecar.
- **Provenance**: `python3 .github/scripts/notebook_provenance.py stamp <nb>.ipynb --executor local-uv --production`. Pre-commit gate rejects a stamped `.ipynb` whose `source_py_blob` ≠ current `.py` (STALE), or `production=False`. Unstamped = advisory only.
- **Checkers**: `uv run python scripts/check_notebook_prose.py <path>` (from `~/ml4t/agents`) — must be 0 violations before commit.
- **Execution**: `uv run jupytext --sync <nb>.py` then `uv run jupyter nbconvert --to notebook --execute --inplace --ExecutePreprocessor.timeout=N <nb>.ipynb`, from the worktree root.
- **CI**: `tests/test_chapter_notebooks.py -k "16_strategy or 17_portfolio"`, papermill overrides from `tests/overrides.yaml`, fixtures seeded by `tests/fixtures/seed_results.py` into `ML4T_OUTPUT_DIR`. CI sets `PLOTLY_RENDERER=json`, which **skips kaleido PNG export and hides export bugs**.

## 3. Files and Code Sections

**Worktree**: `~/ml4t/public-ch16-17`, branch `cs/ch16-17`, based on `origin/main` 9c2a7159. Case-study symlinks added by hand (script was run without `--case-study`):
```
case_studies/{etfs,crypto_perps_funding,cme_futures}/{run_log,labels,features} -> ~/ml4t/code/case_studies/<cs>/<dir>
```

**Chapter 16 — all 18 committed, `fixed`, rendered.** Commits in order: `f3e4a815` (01), `d03e3ea0` (06), `eafc5f08` (01 correction), `b82185eb` (07), `304ea41f` (08), `76b1d974` (09), `db48b3c4` (10), `2045c45c` (11), `54f1dca9` (12), `442304ec` (13), `c8e5fb9d` (14), `f66de317` (15), `37612c13` (16), `4ac9ba6b` (17), `16552d9f` (18).

Highest-value changes:
- `16_strategy_simulation/_etf_baseline.py` — **ported NB01's actual simulator** (next-open fills, sells-first with cash constraint, `fill_dates()` monthly schedule, `metrics()` now calls `ml4t.diagnostic` sharpe/sortino, returns seeded from initial cash). Now reproduces `01_backtest_first_principles` exactly: total 196.7490%, CAGR 8.0935%, Sharpe 0.7434, Sortino 1.0406, MaxDD -31.3429%, final 296,749.02.
- `tests/test_etf_baseline_parity.py` — **new, 7 tests, all passing**. Re-derives the simulation from the notebook's event order; asserts agreement at three cost levels, no borrowing, one return per session, and that Sharpe is *not* `cagr/vol`.
- `tests/overrides.yaml` — added `parameters: {ROLLING_WINDOWS: [5, 10, 21]}` for `16_strategy_simulation/08_signal_method_comparison`.

**Chapter 17 status**:
- `01_portfolio_metrics` — **BLOCKED, unedited, registered** (issue #714). `resolve_best_backtest_runs(stage="allocation")` finds nothing: `case_studies/etfs/run_log/registry.db` has `backtest_runs` = **0 rows** after the run-log reset; `run_log.pre-rebuild/registry.db` holds the 744 allocation rows. Not re-run against the superseded baseline; not edited (editing an unrunnable notebook leaves the pair stale and the gate would reject the commit).
- `02_mean_variance_optimization` — committed `60e3e6eb`, issue #715.
- `03_robust_optimization` — committed `6161d7cd`, issue #716.
- `04_kelly_criterion` — committed `9b0b29cd`, issue #717.
- `05_factor_allocation_evidence` — **IN PROGRESS, currently does not compile** (see §8).
- `08_library_comparison`, `09_allocator_comparison` — **Timestamp fix applied and synced, both execute clean (6 and 13 PNGs), NOT yet committed or teaching-passed.**

The 08/09 fix, isolated by direct test (`Index.tolist()` FAIL / `DatetimeIndex` OK / `to_pydatetime()` OK / `numpy datetime64` OK):
```python
# 08_library_comparison.py
test_dates = test_returns.index        # was .index.tolist()
# 09_allocator_comparison.py, in the result dict
"dates": pd.DatetimeIndex(dates_list), # was dates_list
# plus three downstream pd.to_datetime()/pd.Index() wrappers dropped
```

## 4. Errors and Fixes

- **I wrote "no computed value moved" for ch16/01 and it was wrong.** Sortino moved (0.699163 → 1.040577 momentum; 0.809196 → 1.215868 benchmark) because `ml4t-diagnostic` 0.1.1 computes downside deviation as `sqrt(mean(min(excess,0)**2))` over all observations rather than the stdev of the negative subset. Corrected in a follow-up commit `eafc5f08` and in the register note rather than amending. **This affects every Sortino in both chapters on re-run — state it, don't rediscover it.**
- **ch17/03: my own replacement prose was false.** I wrote that allocations estimating expected returns concentrate most. Printing the full ordering showed Min CDaR (1.48) and Min Variance (1.56) concentrate *harder* than Max Sharpe (1.84). Corrected the section, the takeaway and the figure title. Lesson applied since: check every claim against the executed output.
- **ch17/03 drawdown title** — I first claimed depth of loss separates the allocations most; Sharpe ranges 8x and drawdown 1.4x, so false. Replaced with the true claim (shallowest fall belongs to the weakest performer).
- **ch16/17 harness label** — I briefly replaced `×` (U+00D7) with `x` in `HARNESS_SCENARIO_LABEL`, which would break the live path; restored.
- **ch17/03 `effective_n.index`** — it is a dict, not a Series; AttributeError on execute, fixed to `sorted(effective_n.items(), ...)`.
- **Recurring**: removing `from IPython.display import Markdown, display` before converting *all* call sites → F821. This is the current state of ch17/05.

## 5. Problem Solving

**Both CI failures fixed and verified.** Full chapter-16 suite: `18 passed, 269 deselected in 131.14s`.
1. `08_signal_method_comparison`: `CASE_STUDIES_DIR` ignores `ML4T_OUTPUT_DIR` → `get_case_study_dir(cs, create=False)`. Behind it: a stale pinned whole-file registry SHA (broken locally too), a pinned crypto label `fwd_ret_24h` the registry no longer has (now read from `setup.yaml` primary), IC-based selection (removed — the pipeline doc says IC selects nothing), and no parameters cell at all.
2. `17_backtrader_zipline_engine_parity`: artifact vs harness scenario-label conflation (two constants now), and behind it a `validate_provenance` requiring a `provenance` block the committed artifact has never had (now runs only when present), plus seven hashes pinned against files absent from the repo.

**Filed**: [ml4t/agent-workspace#860](https://github.com/ml4t/agent-workspace/issues/860) — `seed_results.py` overwrites shipped crypto prediction artifacts with a 66-date SYM0..SYM4 grid while the registry keeps `ic_n_days=1956`. Outside ch16-17; reproducer included.

**Deliberately out of scope, flagged to the user, NOT yet filed**: no teaching chapter routes figures through `utils.style.show_plotly_with_alt`, so every rendered teaching page reads "No description has been provided for this image". Corpus-wide across 26 chapters (~145 descriptions, each to be written against a rendered PNG). I told the user I would file an infra issue for it and offered to bring it inside the PR if they prefer. **The issue is still unfiled.**

## 6. All User Messages

1. "Read /home/stefan/ml4t/agents/work/2026-08-20-teaching-ci/prompts/ch16-17.md and follow it. This is your whole assignment; report after every notebook."
2. (System-injected, this turn) Instruction to produce a text-only summary with no tool calls.

Standing constraints from the assignment and global config that must survive compaction: **never bypass a hook** (`--no-verify`, `core.hooksPath`, moving hooks); **never edit `.ipynb` directly** — edit the `.py` and `jupytext --sync`; **never commit `.env`, credentials, or secrets** (a `cat .env` was denied by the sandbox and correctly not retried); **no em dash — plain dash**; **do not push a student-facing repo casually and confirm before any outward-facing action**; **ask Stefan before opening the PR**.

## 7. Pending Tasks

- **ch17/05** — finish (currently broken, see below), execute, stamp, render, commit, register.
- **ch17/06** `06_hierarchical_risk_parity` — executes clean; full pass pending.
- **ch17/08**, **ch17/09** — Timestamp fix in the working tree uncommitted; teaching pass, execute, stamp, render, commit, register.
- **ch17/11, 12, 13** (`11_dl_portfolio_allocation`, `12_vlstm_portfolio`, `13_deepm_regime_robust`) — GPU + tier-skipped in CI; not yet probed. 11 and 07 are already `fixed` from a prior pass and are not in my 24.
- **File the teaching-chapter alt-text infra issue.**
- **Run the full `ch16-17` CI test** (`-k "16_strategy or 17_portfolio"`) once chapter 17 is done.
- **Ask Stefan, then open one PR for the group.**

## 8. Current Work

Mid-pass on `17_portfolio_construction/05_factor_allocation_evidence.py`. Done so far: preamble/objectives rewritten; four computed figure titles replaced; `DISCOVERY_T_THRESHOLD = 3.0` / `CONVENTIONAL_T_THRESHOLD = 2.0` introduced as parameters (replacing hardcoded `> 3.0`, `y=3`, `> 3` in four places) so the prose can name them without tripping the decimals check; three comment-block cells converted to markdown; value-momentum mechanism prose added; takeaways rewritten with a known-limitations list; five `display(Markdown(...))` blocks already converted to `print`.

**The file is currently in a non-compiling state.** `from IPython.display import Markdown, display` was removed, but **five `display(Markdown(...))` call sites remain** and ruff reports 10 × F821. Their locations and content (line numbers as of the last read):

- **~433** `"The aggregate Century panel contributes **{len(century_factors):,} common months** from **{...index.min():%Y-%m} through {...index.max():%Y-%m}**."`
- **~788** SMB pre/post-1981: `ann_return` and `t_stat` for `smb_period_stats['Pre-1981']` and `['1981-present']`, ending "The split was chosen ex post, so it documents decay rather than proving publication caused it."
- **~812** `"The VME panel contains **{len(vme_pd):,} months** and **{len(vme_pd.columns)} published series**."`
- **~972** value-momentum correlation range `min_vme_corr` / `max_vme_corr` / `median_vme_corr` plus the `EVERYWHERE` series value.
- **~1030** `"The common eight-factor panel contains **{len(combined_pl):,} months** from **{...min():%Y-%m} through {...max():%Y-%m}**."`

The notebook has **not** been re-executed, stamped, rendered, committed or registered. Also uncommitted in the working tree: `17_portfolio_construction/08_library_comparison.{py,ipynb}` and `09_allocator_comparison.{py,ipynb}` (Timestamp fix only).

## 9. Optional Next Step

Convert the five remaining `display(Markdown(...))` blocks in `17_portfolio_construction/05_factor_allocation_evidence.py` to `print` (plus a markdown cell where the block carried interpretation, as with the others), then `ruff format` / `ruff check` clean, `check_notebook_prose.py` clean, `jupytext --sync`, execute, stamp `--production`, render, commit, and `register.sh set 17_portfolio_construction/05_factor_allocation_evidence status=fixed`.

This is the direct continuation of the per-notebook loop the assignment specifies — "**The loop, per notebook**: 1. Diagnose once… 3. Re-run and render. 4. **Commit**… 5. **Report after every notebook**" — and the last action taken was reading those five blocks in order to rewrite them.

</summary>
